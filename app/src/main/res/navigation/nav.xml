<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav"
    app:startDestination="@id/mainActivity">

    <activity
        android:id="@+id/mainActivity"
        android:name="dtu.projekt.phonefreedom.MainActivity"
        android:label="activity_main"
        tools:layout="@layout/activity_main" />
    <activity
        android:id="@+id/predefinedMessagesActivity"
        android:name="dtu.projekt.phonefreedom.PredefinedMessagesActivity"
        android:label="activity_predefined_messages"
        tools:layout="@layout/activity_predefined_messages" />
</navigation>